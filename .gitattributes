#Referencia: https://help.github.com/articles/dealing-with-line-endings/
#Para tener el .gitattributes se ejecuto "git config --global core.autocrlf true"

* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
# ejemplo de como agregar archivos para que tenga el salto de linea "*.cgit  text"


